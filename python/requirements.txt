# Prefer versions that ship binary wheels (avoid source builds).
# matplotlib 3.9.2 has no wheel for Python 3.14 and fails when built from source.
# Use Python 3.11 or 3.12 if installs still fail on a very new Python.
pandas>=2.2.3
matplotlib>=3.10.0
numpy>=2.1.0
